name: map_picker_flutter
description: Get the address quickly and easily from Google Map in Web, Desktop and Mobile.
version: 1.1.0+1
homepage: https://github.com/Bestfastfire/map_picker_flutter

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  cached_network_image: ^3.2.0
  rxdart: ^0.27.3
  latlng: ^0.1.0
  dio: ^4.0.4
  map: ^1.0.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
